0-hello_world prints "Hello, World"
1-confused_smiley prints "(Ôo)'
2-hellofile displays the content of /etc/passwd
3-twofiles displays the content of /etc/passwd and /etc/hosts
4-lastlines displays the last 10 lines of /etc/passwd
5-firstlines displays the first 10 lines of /etc/passwd
6-third_line displays the third line of iacta
7-file creates the file \*\\'"Best School"\'\\*$\?\*\*\*\*\*:) containing the text Best School
8-cwd_state writes the the result of ls -la into the file ls_cwd_content
9-duplicate_last_line duplicates the last line of iacta
10-no_more_js deletes all .js files in the current directory and subfolders
11-directories counts the number of directories and sub-directories in the curretn directory, excluding it
12-newest_files displays the 10 newest files in the directory
13-unique prints only words that appear exactly once
14-findthatword displays lines containing root from /etc/passwd
15-countthatword displays the number of lines that contain bin in /etc/passwd
16-whatsnext displays lines containing the word root and three lines after them in /etc/passwd
17-hidethisword display lines that do not contain bin in /etc/passwd
18-letteronly display lines in /etc/ssh/sshd_config starting with a letter
19-AZ replaces letters A and c with Z and e respectively
20-hiago removes letters c and C
21-reverse reverses script input
22-users_and_homes displays all users and home directories, sorted by user
23-empty_casks finds all empty files and directories in current directory and sub-directories
24-gifs list all .gif files in the current directory and sub-directories, including hidden files, without extensions, sorted by byte values
25-acrostic decodes an acrostic
26-the_biggest_fan displays 11 hosts from a TSV log from most active
